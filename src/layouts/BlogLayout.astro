---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  date: string;
  category: string;
}

const { title, date, category } = Astro.props;
---

<BaseLayout title={title}>
  <div class="reading-progress fixed top-0 left-0 h-1 bg-neon-green z-50" id="reading-progress"></div>

  <article class="max-w-3xl mx-auto px-4 py-16">
    <header class="mb-12">
      <span class="inline-block px-3 py-1 text-sm font-medium bg-neon-green/20 text-neon-green rounded-full mb-4">
        {category}
      </span>
      <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">{title}</h1>
      <time class="text-text-dim/60">{date}</time>
    </header>

    <div class="prose prose-invert prose-lg max-w-none
      prose-headings:text-white
      prose-p:text-text-dim
      prose-a:text-neon-green prose-a:no-underline hover:prose-a:text-neon-green
      prose-strong:text-white
      prose-code:text-neon-green prose-code:bg-card prose-code:px-2 prose-code:py-1 prose-code:rounded
      prose-pre:bg-card prose-pre:border prose-pre:border-neon-green/10
      prose-blockquote:border-l-neon-green prose-blockquote:text-text-dim/80
      prose-li:text-text-dim">
      <slot />
    </div>
  </article>

  <script>
    const progressBar = document.getElementById('reading-progress');

    window.addEventListener('scroll', () => {
      const scrollTop = window.scrollY;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const progress = (scrollTop / docHeight) * 100;
      if (progressBar) {
        progressBar.style.width = `${progress}%`;
      }
    });
  </script>
</BaseLayout>
